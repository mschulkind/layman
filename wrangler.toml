name = "layman"
compatibility_date = "2026-02-01"

[assets]
directory = "site/dist"

[build]
command = "pnpm install -C site && pnpm -C site build"

[env.production]
# Specify exact runtime versions for production builds
# Node 22, pnpm 9 (defined here instead of relying on unreliable
# .tool-versions detection by Cloudflare)

